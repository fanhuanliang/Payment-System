# change the file name to .env, and put a string in FILL_ME_IN below.
# use command lines below to generate the secret token or you could make your own key

#node 
#require('crypto-random-string').randomBytes(64).toString('hex')
ACCESS_TOKEN_SECRET = 'FILL_ME_IN'
REFRESH_TOKEN_SECRET = 'FILL_ME_IN'